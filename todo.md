# 員工打卡系統 App - TODO

## 後端 API

- [x] 設計資料庫 schema（employees, attendance, schedules, work_shifts, settings, devices）
- [x] 員工認證 API（登入、JWT token、修改密碼）
- [x] 員工管理 API（CRUD）
- [x] 打卡 API（上班打卡、下班打卡、查詢紀錄）
- [x] 排班 API（查詢、新增、修改）
- [x] 班表模板 API（CRUD）
- [x] 系統設定 API（公司資訊、GPS 設定、打卡規則）
- [x] 裝置綁定 API（綁定、查詢、刪除）
- [x] 請假申請 API（申請、審核、查詢）

## 員工端 App

### 認證流程
- [x] 登入頁面（帳號密碼）
- [x] JWT token 儲存（SecureStore）
- [x] 自動登入（token 持久化）
- [x] 登出功能

### 首次設定精靈
- [x] 步驟 1：修改密碼
- [x] 步驟 2：裝置綁定
- [x] 步驟 3：生物識別設定（expo-local-authentication）
- [x] 步驟 4：完成頁面

### 打卡主頁
- [x] 當前時間顯示（大字）
- [x] 今日班表顯示（最多 2 班）
- [x] 打卡狀態指示（已打卡/未打卡）
- [x] 上班打卡按鈕（GPS + 生物識別）
- [x] 下班打卡按鈕（GPS + 生物識別）
- [x] 打卡成功動畫與回饋
- [x] 今日打卡紀錄顯示

### 打卡紀錄頁
- [x] 最近 30 天打卡卡片列表
- [x] 每日打卡狀態顯示

### 請假申請頁
- [x] 請假申請表單
- [x] 請假類型選擇
- [x] 申請狀態查詢
- [x] 取消待審假單

### 個人資料頁
- [x] 個人資訊顯示
- [x] 裝置管理（解除綁定）
- [x] 修改密碼
- [x] 登出

## 管理後台

### 認證
- [x] 管理員登入頁面（共用員工登入，角色判斷）
- [x] 角色權限判斷（admin/employee）

### 管理首頁
- [x] 今日出勤概覽（統計卡片）
- [x] 今日打卡紀錄列表
- [x] 待審假單提示

### 員工管理
- [x] 員工列表（搜尋、篩選）
- [x] 新增員工
- [x] 編輯員工資料
- [x] 刪除員工
- [x] 重置員工密碼
- [x] 啟用/停用員工

### 打卡紀錄管理
- [x] 所有員工打卡紀錄列表
- [x] 日期/員工篩選
- [x] 單筆刪除
- [x] 批量刪除

### 排班管理
- [x] 週視圖排班
- [x] 手動設定班次

### 系統設定
- [x] 公司資訊設定（名稱、GPS 座標、允許半徑）
- [x] 打卡規則設定（是否需要裝置綁定、GPS）
- [x] 工作時段管理（CRUD）
- [x] 裝置管理（查詢、解除綁定）
- [x] 請假審核（核准/拒絕）

## 品牌與 UI

- [x] 更新主題色彩（深藍色系 #1E40AF）
- [x] 生成 App 圖示（時鐘+打勾設計）
- [x] 更新 app.config.ts 品牌資訊
- [x] 底部 Tab Bar 設定（員工端：打卡/紀錄/請假/個人）
- [x] 底部 Tab Bar 設定（管理後台：總覽/打卡紀錄/員工/排班/設定）

## Bug 修復
- [x] schedules.getToday 回傳 undefined 導致 TanStack Query 錯誤
- [x] SystemSettings 在 render 期間直接 setState 的問題

## 新功能 - UI 改版
- [x] 登入頁：加入「記住帳號密碼」 checkbox
- [x] 登入頁：加入「保持登入」 checkbox（持久化 session）
- [x] 管理後台：重新設計儀表板排版（對齊網頁版）
- [x] 管理後台：重新設計員工管理排版
- [x] 管理後台：重新設計打卡紀錄排版
- [x] 管理後台：重新設計排班管理排版
- [x] 管理後台：重新設計設定頁排版
- [x] 員工端：重新設計打卡主頁排版
- [x] 員工端：重新設計打卡紀錄排版
- [x] 員工端：重新設計請假申請排版
- [x] 員工端：重新設計個人資料排版

## 排班管理改版
- [x] 排班管理頁：顯示完整排班表（含每班次時間、員工名稱）
- [x] 排班管理頁：週視圖每格顯示完整時間（上班/下班時間）
- [x] 排班管理頁：快速套用已建立的工作時段
- [x] 排班管理頁：支援刪除排班

## Tab Bar 改版
- [x] 員工端 Tab Bar：標籤移到圖示左側（橫向排列）
- [x] 管理後台：改為左側導航列（漢堡選導檢單）

## 管理後台左側導航列改版
- [x] 建立左側垂直導航列元件（深藍色背景，對齊網頁版）
- [x] 管理後台改為 Stack 架構，移除底部 Tab Bar
- [x] 新增請假審核頁面
- [x] 新增報表統計頁面
- [x] 新增工作時段管理頁面（從設定頁獨立）
- [x] 新增裝置管理頁面（從設定頁獨立）
- [x] 新增員工打卡頁入口（管理員也可查看打卡頁）

## 時間輸入改版
- [ ] 建立滾輪式時間選擇器元件（小時/分鐘雙滾輪）
- [ ] 排班管理：班次時間改為滾輪選擇器
- [ ] 工作時段管理：上下班時間改為滾輪選擇器

## 月曆式排班總覽
- [ ] 建立滾輪式時間選擇器元件（小時/分鐘雙滾輪）
- [ ] 排班管理：班次時間改為滾輪選擇器
- [ ] 工作時段管理：上下班時間改為滾輪選擇器
- [ ] 管理後台：新增月曆式排班總覽頁（可切換員工查看）
- [ ] 管理後台：月曆每格顯示當日班次時間
- [ ] 員工端：新增月曆式排班總覽頁（查看自己當月排班）
- [ ] 員工端：月曆每格顯示當日班次時間

## 儀表板互動改版
- [ ] 管理後台儀表板：統計卡片點擊跳轉對應頁面（在職員工→員工管理、已打卡→打卡紀錄、遲到→打卡紀錄、待審假單→請假審核）

## 排班請假標記功能（完整版）
- [ ] 資料庫 schema：schedules 表加入 leaveType、leaveMode（allDay/partial）、leaveStart、leaveEnd、leaveDuration 欄位
- [ ] 後端 API：upsert/getByEmployee/getWeekAll 支援請假欄位
- [ ] 建立滾輪式時間選擇器元件（小時/分鐘雙滾輪）
- [ ] 排班管理頁：格子可標記請假種類（特休/病假/事假/婚假/喪假/公假/其他）
- [ ] 排班管理頁：請假模式選擇（整天 / 指定時段）
- [ ] 排班管理頁：指定時段用滾輪選時間，自動計算請假時長
- [ ] 排班管理頁：班次時間輸入改為滾輪式選擇器
- [ ] 月曆總覽頁：顯示請假種類和時長（彩色徽章）
- [ ] 員工端月曆排班總覽頁：顯示自己的排班和請假
- [ ] 員工端月曆每格顯示班次時間或請假種類/時長

## 公開連結 + 打卡三重驗證
- [x] 路由守衛改版：isLoading 時顯示 loading spinner（不再顯示空白頁）
- [x] 任何人開啟連結都能看到登入頁（不再顯示 Not Found 或空白）
- [x] 打卡三重驗證：生物識別（Face ID/指紋）+ 裝置綁定 + GPS 定位
- [x] 後端 clockIn 強化：require_device_binding=true 時必須提供 deviceId 且已綁定
- [x] 後端 clockOut 強化：同樣強制裝置綁定驗證
- [x] 打卡頁面顯示驗證進度 overlay（生物識別中/裝置確認中/定位中/打卡記錄中）
- [x] 打卡頁面顯示安全驗證說明卡片

## 外部平台部署（Railway）
- [ ] 修改後端伺服器同時提供前端靜態檔案
- [ ] 建立 Dockerfile 和 railway.json 部署設定
- [ ] 前端 API URL 改為可配置環境變數
- [ ] 建立 GitHub 倉庫並上傳程式碼
- [ ] 引導用戶在 Railway 完成部署
## 推播通知功能（Web Push）
- [x] 資料庫 pushSubscriptions 表新增 employeeId 欄位（null = 管理員訂閱）
- [x] db.ts 新增 getPushSubscriptionsByEmployee 和 getAdminPushSubscriptions 函數
- [x] push router subscribe 支援 employeeId 參數
- [x] cron-jobs.ts：每分鐘掃描排班，未打卡提醒（管理員）和打卡前5分鐘提醒（員工）
- [x] server/_core/index.ts 整合 startCronJobs()
- [x] 管理員設定頁新增「未打卡提醒」開關和延遲分鐘數設定
- [x] 管理員設定頁新增「打卡前提醒（員工）」開關
- [x] 員工打卡頁新增推播訂閱按鈕（僅在管理員開啟打卡前提醒時顯示）

## 單一裝置綁定功能
- [x] devices schema 新增 status 欄位（approved/pending/rejected）
- [x] 執行 migration 更新資料庫
- [x] db.ts 新增 getPendingDevices、updateDeviceStatus、countApprovedDevicesByEmployee 函數
- [x] devices.register API：一般員工已有核准裝置時，新裝置自動設為 pending 並推播通知管理員
- [x] devices.register API：管理員（role=admin）和職稱「M」的員工不受限制，直接核准
- [x] devices.getPending API：查詢所有待審核裝置
- [x] devices.review API：管理員審核裝置（核准/拒絕）
- [x] 管理後台裝置管理頁：新增「待審核」/「全部裝置」分頁切換
- [x] 管理後台裝置管理頁：待審核裝置顯示核准/拒絕按鈕
- [x] 管理後台裝置管理頁：全部裝置顯示狀態徽章和多裝置標示
- [x] 員工端裝置綁定頁：返回 pending 時顯示等待審核畫面
- [x] 員工端打卡頁：裝置為 pending 或 rejected 時阻止打卡並顯示提示

## 裝置管理頁面改善
- [x] 裝置管理頁：將「裝置 ID」改為顯示「裝置名稱」（型號）

## 系統設定頁整理
- [x] 移除系統設定內的「工作時段」分頁（保留左側標籤頁）
- [x] 移除系統設定內的「裝置管理」分頁（保留左側標籤頁）
- [x] 移除系統設定內的「請假審核」分頁（保留左側標籤頁）
- [x] 系統設定只保留「系統設定」單一分頁
- [x] 左側標籤頁新增「工作時段」獨立頁面

## 週排班頁面改善
- [x] 週排班頁面：新增當天內場、外場、幹部（M/管理員）排班人數統計區塊

## 緊急修復
- [x] 打卡頁面「裝置綁定中」一直轉圈，所有員工無法打卡

## 裝置管理簡化
- [x] server 端：移除單一裝置數量限制，所有新裝置自動設為 approved
- [x] client 端：移除 pending/rejected 阻擋打卡畫面
- [x] 保留管理員查看/移除裝置功能

## 登出修復
- [x] 員工帳戶登出按鈕沒有反應

## GPS 定位修復
- [x] GPS 定位加入 10 秒 timeout，超時後顯示錯誤訊息，不讓使用者無限轉圈

## GPS timeout 二次修復
- [x] 修復裝置綁定一直轉圈：Railway MySQL devices 表缺少 status 欄位，執行 ALTER TABLE 修復

## 打卡錯誤提示修復
- [ ] 修復 handleClock catch 區塊靜默吃掉錯誤，打卡失敗時正確顯示錯誤訊息

## GPS Web 定位修復
- [ ] 改用 Web 原生 navigator.geolocation（有 timeout 參數），解決 Expo Location 在 Web 上永遠卡住的問題

## GPS 範圍錯誤提示修復
- [x] GPS 超出範圍時顯示明確錯誤訊息並停止轉圈

## 打卡錯誤訊息徹底修復
- [x] 確認 tRPC 錯誤物件結構，修復 GPS/IP 失敗時無法顯示錯誤訊息的問題（改用 mutate + UI 直接顯示錯誤）

## 打卡錯誤訊息最終修復
- [x] 加入 server debug log 確認請求流程
- [x] 重寫 client 端錯誤顯示，使用最可靠的方式確保任何情況都能顯示

## 早退判斷修復
- [ ] 修復早退判斷邏輯：班次 11:00-14:00，下班打卡 14:05 不應顯示早退（時區或判斷邏輯問題）
- [ ] 員工當天多個班次時，分別對應各班次判斷遲到/早退/正常

## 打卡靜默失敗修復（陳宇心案例）
- [x] 修復 GPS 錯誤碼處理：Web geolocation err.code 2（POSITION_UNAVAILABLE）未被處理，導致靜默失敗
- [x] 修復 requireGPS 判斷：同時讀取 require_gps 設定開關，不再只看座標是否存在
- [x] 修復 requireGPS 保守策略：settings 未載入時視為需要 GPS，避免跳過驗證
- [x] 改善 parseTrpcError：過濾 INTERNAL_SERVER_ERROR 等非使用者友善訊息，顯示更明確的提示
- [x] 統一 GPS 取得邏輯：合併 requireGPS/非 requireGPS 路徑，避免靜默吞掉錯誤

## 驗證方式改版：移除裝置綁定，改為 GPS + 生物識別 + WiFi
- [ ] 前端：移除打卡流程中的裝置綁定步驟（Step 2 device）
- [ ] 前端：更新打卡安全驗證說明卡片（移除裝置綁定說明，改為 WiFi 說明）
- [ ] 前端：更新 VerifyStep 類型，移除 device 步驟
- [ ] 後端 clockIn：移除裝置綁定驗證邏輯
- [ ] 後端 clockOut：移除裝置綁定驗證邏輯
- [ ] 管理員設定頁：移除「裝置綁定」開關，確保 WiFi IP 白名單設定明顯易用

## LINE OTP 驗證打卡（取代裝置綁定）
- [ ] 設定 LINE_CHANNEL_SECRET 和 LINE_CHANNEL_ACCESS_TOKEN 環境變數
- [ ] 後端：資料庫新增 line_user_id 欄位到 employees 表
- [ ] 後端：新增 sendLineOtp API（發送 6 位數驗證碼到員工 LINE）
- [ ] 後端：新增 verifyLineOtp API（驗證 OTP，回傳 clockToken）
- [ ] 後端：新增 bindLine Webhook（員工加入 Bot 後自動記錄 userId）
- [ ] 後端：修改 clockIn/clockOut 改用 clockToken 驗證，移除裝置綁定驗證
- [ ] 前端：打卡流程移除裝置綁定步驟，加入 LINE OTP 輸入步驟
- [ ] 前端：員工個人資料頁新增「綁定 LINE 帳號」入口（顯示 Bot QR Code）

## 打卡強制自拍功能（防代打卡）

- [x] 資料庫 attendance 表新增 clockInPhoto / clockOutPhoto 欄位
- [x] 後端 clockIn / clockOut API 支援 photoBase64 + photoTimestamp
- [x] 後端驗證照片時間戳（30 秒有效期，防止上傳舊照片）
- [x] 後端照片上傳至 S3 並儲存 URL
- [x] 前端打卡頁面強制開啟即時相機串流（getUserMedia，不允許上傳檔案）
- [x] 照片加入時間戳記浮水印（UTC+8 時間）
- [x] 管理員打卡記錄頁面加入照片查看功能（點擊記錄查看上班/下班照片）
- [x] 移除裝置綁定相關 UI（profile 頁面、管理員設定頁面）
- [x] 移除 LINE OTP 相關 API 掛載

## 照片自動清除

- [x] 後端新增每日定時清除 7 天前打卡照片的 cron job（S3 刪除 + DB 欄位清空）

## 返回鍵與登出修復

- [x] 所有頁面加返回鍵（員工端：打卡記錄、請假、個人資料等子頁面）
- [x] 管理後台所有頁面加返回鍵
- [x] 列印預覽畫面加返回鍵
- [x] 修復員工帳號登出按鈕無反應問題
- [x] 打卡記錄加狀態篩選（遲到、早退、未打卡、正常等）
- [x] 打卡記錄改為同一員工同一天上班/下班並列顯示（一行一天）
- [x] 報表匯出改為 Excel 格式（.xlsx）

## 排班人力統計修復

- [x] 修復排班頁面內場/外場/幹部人力統計顯示 0 的問題（員工分類欄位未正確對應）

## 打卡照片縮圖預覽

- [x] 管理後台打卡記錄列表直接顯示上班/下班照片縮圖（無需點開即可預覽）
- [x] 點擊縮圖可放大查看完整照片

## 報表 Excel 匯出修復

- [x] 修復報表匯出只出現 CSV 選項，無法匯出 .xlsx 格式的問題

## 打卡記錄頁面修復

- [x] 筛選按鈕（全部/正常/遲到/早退/缺勤/未下班）不顯示（Safari 快取問題，強制 entry JS hash 改變）
- [x] 狀態判斷錯誤：遲到/早退顯示為正常（後端 getGrouped 動態重新計算狀態）

## 打卡記錄 SSR 版本（解決 Service Worker 快取問題）

- [x] 後端新增 /attendance-v2 路由，直接產生 HTML，不依賴前端 JS 快取
- [x] 支援日期篩選、員工搜尋、狀態篩選（全部/正常/遲到/早退/缺勤）
- [x] 同員工同天合併顯示（多班次並列）
- [x] 狀態正確判斷（遲到/早退/正常）
- [x] 18 個單元測試全部通過

## SSR 路由整合（原本連結直接使用）

- [x] 將 SSR 路由改為同時攔截 /admin/attendance（原本路由）和 /attendance-v2
- [x] SPA fallback 加入 /admin/attendance 例外，讓後端 SSR 優先回應

## 管理後台打卡記錄修復（在後台內顯示）

- [x] 移除 SSR 對 /admin/attendance 的攔截（讓前端頁面正常顯示）
- [x] 改用 getGrouped API，同員工同天合併顯示
- [x] 加入篩選按鈕（全部/正常/遲到/早退/缺勤/未下班打卡）
- [x] 狀態動態計算（不依賴資料庫舊值）
- [x] 重新 build 前端靜態檔案
